# ================================================================
# requirements.txt - Python Package Dependencies
# ================================================================
# Install all packages with:
#   pip install -r requirements.txt
#
# To generate this file from your current environment:
#   pip freeze > requirements.txt

# Core Framework
Django==5.0.6

# Django REST Framework - turns Django into a REST API
djangorestframework==3.15.2

# Filtering support for DRF (used for query parameter filtering)
django-filter==24.2

# Optional but recommended for production:
# gunicorn==21.2.0        # Production WSGI server
# psycopg2-binary==2.9.9  # PostgreSQL adapter
# python-decouple==3.8    # Load settings from .env file
# whitenoise==6.7.0       # Serve static files efficiently
